name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      -name: print
      run: echo "Hello world"
      -name: print goodbye
      run: echo "Goodbye"
